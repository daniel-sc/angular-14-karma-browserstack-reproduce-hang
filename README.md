# Reproduction for non-terminating Angular 14 Karma tests with karma-browserstack-launcher

## Reproduce

```shell
npm install
npm run test-ci
```

## Observe
`npm run test-ci` does not terminate (whereas `npm run test` does).
